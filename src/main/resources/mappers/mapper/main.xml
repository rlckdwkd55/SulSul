<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sulsulmarket.sulsul.main.mapper.MainMapper">

    <select id="getBestRankingProd" resultType="com.sulsulmarket.sulsul.main.dto.Product">
        SELECT * FROM PRODUCT ORDER BY PAYMENT_COUNT DESC LIMIT 10;
    </select>

    <select id="getNewProduct" resultType="com.sulsulmarket.sulsul.main.dto.Product">
        SELECT * FROM PRODUCT ORDER BY UPDATE_DATE DESC LIMIT 8;
    </select>

</mapper>